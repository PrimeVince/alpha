let speed = 45;
let message = speed >= 50 ? `your over speeding` : `Your observing the speed limit`;
console.log(message)


let time = 5.3;
let greeting = time <= 12 ? `Good morning` : `Good afternoon`;
console.log(greeting)

let istudent = false;
let expectation = istudent ? `you should be studiying` : `You should be working`;
console.log(expectation)


let total =345;
let discount = total >= 101 ? 9 : 0;
console.log(`your total is $${total - (total * (discount/100))}`);

